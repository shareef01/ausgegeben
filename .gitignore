# Android
*.iml
.gradle
.kotlin
/local.properties
/.idea/
.externalNativeBuild
.cxx
*.apk
*.ap_
*.dex
*.class
bin/
gen/
out/
.navigation/
captures/
*.hprof
app/google-services.json

# Release signing (see keystore.properties.example)
keystore.properties
*.jks
*.keystore

# Android SDK (local dev only — never commit)
android-sdk/

# Build outputs
/build
/app/build

# OS
.DS_Store

# Firebase local
.firebase/
firebase-debug.log
firestore-debug.log
# Optional local override; see firebase.emulator.json.example
firebase.emulator.json

# Local audit harness (Playwright feature audit — local only, never committed)
web/.audit-*.mjs
web/.audit-shots/
web/.inspect-*.mjs

# Node (any location — main once had node_modules committed at the root)
node_modules/

# Claude Code personal settings
.claude/

# Web
web/node_modules/
web/dist/
web/.env.local
web/.env.production
web/.env.emulator
web/.browser-key-rotation.json
web/.vite/
web/*.tsbuildinfo

# Agent / editor
.agent/
.artifacts/
.cursor/
.reasonix/

# Obsolete local helper (replaced by scripts/strip-commit-trailers.py)
scripts/_strip_coauthor.py

# Wrangler local state — caches the Cloudflare account id and name
tools/error-endpoint/.wrangler/

# Internal audit reports — kept locally, not published.
# These describe account data shapes and review findings; the repo is public.
AUDIT.md
AUDIT_REPORT.md
docs/audit-baseline.md
docs/audit-summary.md
