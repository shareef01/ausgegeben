# Copy to keystore.properties (gitignored) or set AUSGEGEBEN_* env vars in CI.
# Paths are relative to the repo root unless absolute.
storeFile=path/to/ausgegeben-release.jks
storePassword=
keyAlias=ausgegeben
keyPassword=
