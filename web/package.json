{
  "name": "ausgegeben-web",
  "private": true,
  "version": "2.0.6",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "lint": "tsc --noEmit",
    "lint:css": "node scripts/check-css-classes.mjs",
    "test": "vitest run",
    "test:prod-env": "node --test scripts/prod-env.test.mjs",
    "test:watch": "vitest",
    "test:rules": "npx firebase emulators:exec --only firestore --project demo-ausgegeben-rules \"vitest run --config vitest.rules.config.ts\"",
    "test:emulator": "npx firebase emulators:exec --only firestore --project demo-ausgegeben-repo \"vitest run --config vitest.emulator.config.ts\"",
    "icons": "node scripts/generate-icons.mjs",
    "prebuild": "npm run icons",
    "validate:prod-env": "node scripts/validate-prod-env.mjs",
    "validate:prod-bundle": "node scripts/validate-prod-env.mjs --bundle",
    "deploy": "npm run validate:prod-env && npm run build && npm run validate:prod-bundle && npx firebase deploy --only hosting:aus01,firestore:rules,firestore:indexes --config ../firebase.json && npm run smoke",
    "deploy:hosting": "npm run validate:prod-env && npm run build && npm run validate:prod-bundle && npx firebase deploy --only hosting:aus01 --config ../firebase.json",
    "deploy:rules": "npx firebase deploy --only firestore:rules,firestore:indexes --config ../firebase.json",
    "smoke": "node scripts/smoke.mjs"
  },
  "dependencies": {
    "firebase": "^12.16.0",
    "lucide-react": "^1.43.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "zustand": "^5.0.5"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "@types/react": "^19.1.2",
    "@types/react-dom": "^19.1.2",
    "@vitejs/plugin-react": "^6.0.3",
    "fake-indexeddb": "^6.2.5",
    "firebase-tools": "^15.24.0",
    "playwright": "^1.61.1",
    "sharp": "^0.35.2",
    "typescript": "~7.0.2",
    "vite": "^8.1.5",
    "vite-plugin-pwa": "^1.3.0",
    "vitest": "^4.1.10"
  },
  "overrides": {
    "brace-expansion": "5.0.9",
    "minimatch": "10.2.6",
    "qs": "6.16.0"
  }
}
